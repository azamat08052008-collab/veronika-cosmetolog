{
  "name": "veronika-cosmetolog",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "prebuild": "node scripts/sync-data.mjs",
    "build": "vite build",
    "preview": "vite preview",
    "deploy": "npm run build && cd dist && git init -q && git checkout -q -b gh-pages && git add -A && git commit -q -m 'Сборка сайта' && git push -q -f https://github.com/azamat08052008-collab/veronika-cosmetolog.git gh-pages && cd .. && rm -rf dist/.git"
  },
  "dependencies": {
    "react": "^19.1.0",
    "react-dom": "^19.1.0"
  },
  "devDependencies": {
    "@vitejs/plugin-react": "^4.4.1",
    "vite": "^6.3.5"
  }
}
